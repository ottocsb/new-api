{
  "When enabled, the client only receives a generic message when this channel errors; the original error is still recorded in logs.": "When enabled, the client only receives a generic message when this channel errors; the original error is still recorded in logs.",
  "Comma-separated channel IDs to skip during scheduled and bulk channel tests. Useful for auxiliary image channels where testing is costly and likely to fail.": "Comma-separated channel IDs to skip during scheduled and bulk channel tests. Useful for auxiliary image channels where testing is costly and likely to fail."
}
